/**
 * Enum representing the type of view currently active.
 *
 * - FEED: List-style feed view showing reviews in a scrollable format.
 * - MAP: Map-based view displaying reviews as location-based markers.
 */
enum ViewType {
  FEED,
  MAP,
}

export default ViewType;
