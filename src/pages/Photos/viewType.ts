/**
 * Enum representing the view types used in the application.
 *
 * @enum {number}
 * @property {number} MODAL - Represents the modal view type, typically used for displaying detailed or enlarged content.
 * @property {number} THUMBNAIL - Represents the thumbnail view type, typically used for displaying smaller, preview-sized content.
 */
enum ViewType {
  MODAL,
  THUMBNAIL,
}

export default ViewType;
